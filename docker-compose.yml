version: '2'
services: 

  blog-db:
    build: 
      context: ./db
    container_name: blog-db
    stdin_open: true
    tty: true
    volumes:
      - blog-data:/blog-data

volumes:
  blog-data:
    driver: local
